// generated by react-router-hono-server/dev

import { Hono } from "hono"
import { createHonoServer } from "react-router-hono-server/bun"

const app = new Hono()

app.get("/health", (c) =>
	c.json({
		status: "ok",
		mode: process.env.MODE,
		timestamp: Date.now(),
	}),
)

// called by vite config
export default await createHonoServer({
	app,
})
